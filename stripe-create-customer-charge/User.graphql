type User implements Node {
  createdAt: DateTime!
  id: ID! @isUnique
  email: String!
  nameFirst: String!
  nameLast: String!
  isPaid: Boolean! @defaultValue(value: false)
  stripeCustomerId: String
  stripeToken: String
  updatedAt: DateTime!
}
