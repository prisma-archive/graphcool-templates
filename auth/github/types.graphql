type GithubUser {
  # required system field:
  id: ID! @isUnique

  # optional system fields. uncomment if you need them:
  # createdAt: DateTime!
  # updatedAt: DateTime!

  githubUserId: String @isUnique
}
