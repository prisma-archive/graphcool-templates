# GraphQL types
types: ./types.graphql

# functions
functions:
  authenticate:
    handler:
      code:
        src: ./src/authenticate.js
        environment:
          AUTH0_SECRET: ${env:BACKEND_AUTH0_SECRET}
          AUTH0_DOMAIN: ${env:BACKEND_AUTH0_DOMAIN}
    type: resolver
    schema: ./src/authenticate.graphql
