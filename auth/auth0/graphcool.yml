# GraphQL types
types: ./types.graphql

# functions
functions:
  authenticate:
    handler:
      code:
        src: ./src/authenticate.js
        environment:
          AUTH0_DOMAIN: ${env:AUTH0_DOMAIN}
          AUTH0_CLIENT_ID: ${env:AUTH0_DOMAIN}
    type: resolver
    schema: ./src/authenticate.graphql
