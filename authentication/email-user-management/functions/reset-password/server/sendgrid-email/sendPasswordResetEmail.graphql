subscription {
  User(filter: {
    mutation_in: [UPDATED]
    updatedFields_contains: "resetToken"
  }) {
    updatedFields
    node {
      id
      email
      resetToken
      firstName
      lastName
    }
  }
}
